import React from 'react'

// Require About component style
require('./style.sass');

export default () => {
  return(
    <div id="about">
      <h2 className="page-title">About Page</h2>
    </div>
  )
};
