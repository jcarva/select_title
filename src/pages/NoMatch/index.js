import React from 'react'

// Require NoMatch component style
require('./style.sass');

export default () => {
  return(
    <div id="no-match">
      <h2 className="page-title">No Match</h2>
    </div>
  )
};
